/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: [
        'firebasestorage.googleapi.com',
        'm.media-amazon.com',
      ],
    },
  };
  
  module.exports = nextConfig;
  
